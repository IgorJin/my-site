{
  "name": "digital-card",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @digital-card/web",
    "build": "npm run build --workspace @digital-card/web",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint apps/web"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.4",
    "prettier": "^3.9.6",
    "typescript-eslint": "^8.67.0"
  }
}
